import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// user should be able to search for their favorite player 
//
// save their favorite players to a dream team page 
// each player on the dream team page is displayed like a playing card